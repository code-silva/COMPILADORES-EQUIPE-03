# 📖Glossário de Termos 
 Esta documentação reúne os termos e conceitos utilizados no desenvolvimento do **PyToJava**, com o objetivo de centralizar e organizar essas definições em um único material de consulta. Assim, busca-se facilitar o entendimento dos conceitos empregados no projeto e servir como apoio para o esclarecimento de dúvidas durante o desenvolvimento.

| Termo | Categoria | Descrição | Exemplo Válido | Observação |
| :--- | :--- | :--- | :--- | :--- |
| **Lexema** | Estrutura Léxica | Texto literal do código-fonte que corresponde a um padrão reconhecido pelo analisador léxico. | `str lexema = "Olá mundo"`<br> os lexemas são: `str`, `lexema`, `=`, `"Olá mundo"`. | Vários lexemas diferentes podem gerar o mesmo tipo de token. |
| **Literal** | Estrutura Léxica | Valor nativo extraído do lexema, sem a sintaxe da linguagem (sem aspas, sem escapes). Representa o dado real contido no token. | `x = "Olá mundo"`<br>`literal = Olá mundo` (sem aspas)<br>`y = 42`<br>`lexeme = "42"` `literal = 42` (int) | Fica `None` para tokens que não carregam valor, como palavras reservadas e operadores. |
| **Token** | Estrutura Léxica | Sequência de caracteres que contém um nome abstrato representando o grupo do lexema reconhecível com significado. | `token_if = Token(type=TokenType.KW_IF, lexeme="if", literal=None, line=2, column=1)` | Linha e coluna começam em 1. O `lexeme` guarda o texto exato; o `literal`, o valor nativo; e ambos podem ser `None` conforme o tipo. |
| **INDENT** | Estrutura Léxica | Token artificial gerado pelo lexer (não existe no texto-fonte) quando há aumento de indentação. Sinaliza ao parser o início de um novo bloco. | `if True:`<br> `␣␣␣␣print("oi")`<br> o lexer emite `INDENT` antes de `print`. | Injetado pelo lexer; é token, não nó sintático. |
| **DEDENT** | Estrutura Léxica | Token artificial gerado pelo lexer (não existe no texto-fonte) quando há redução de indentação. Sinaliza ao parser o fim de um bloco. | `if True:`<br> `␣␣␣␣print("oi")`<br>`print("fim")`<br> o lexer emite `DEDENT` antes de `print("fim")`. | Sempre pareado com um INDENT anterior. Pode emitir vários ao final do arquivo. |
| **Lookahead** | Estrutura Léxica | Técnica em que o analisador examina o próximo símbolo sem consumi-lo, para decidir qual regra aplicar. | `x = 1`<br>`y = 2`<br>ao ler `=`, o lexer olha o próximo caractere: se for outro `=`, forma `==`, senão, mantém `=`. | O símbolo observado não é consumido: continua disponível para leitura posterior. Só após a decisão o cursor avança. |
| **Associatividade** | Análise Sintática | Especifica a ordem de avaliação de operadores em situações de precedência igual (esquerda, direita ou nenhuma). | `%left PLUS MINUS`<br>`%left TIMES DIVIDE`<br><br>Conta: `x + 2 / 3`<br> como `TIMES DIVIDE` têm precedência maior, o parser avalia primeiro `2 / 3`:<br>`x + (2 / 3)` | No Yacc, é declarada com `%left` (esquerda), `%right` (direita) ou `%nonassoc` (nenhuma), bem como é definida pela ordem das linhas: quanto mais abaixo, maior a precedência. |
| **Símbolo terminal** | Análise Sintática | Símbolo básico da gramática, equivalente a um token reconhecido pelo analisador léxico. | `1 + 2`<br> terminais: `NUM` (`1`), `PLUS` (`+`), `NUM` (`2`). | Não pode ser expandido. Representa a folha da árvore sintática. |
| **Símbolo não terminal** | Análise Sintática | Símbolo abstrato utilizado para definir regras gramaticais, representando construções complexas da linguagem. | `1 + 2`<br> não terminal envolvido: `expr`. | Precisa ser expandido por regras até virar terminais. Nunca aparece no código-fonte. |
| **`$$`, `$1`, `$2`...** | Análise Sintática | Referências aos valores envolvidos em uma regra gramatical durante a ação semântica. `$$` representa o resultado da produção; `$1`, `$2`, ... representam os valores dos símbolos do lado direito, na ordem em que aparecem. | `expr : expr PLUS expr`<br>`$1` = valor do 1º `expr`; `$2` = valor do `PLUS`; `$3` = valor do 2º `expr`; `$$` = resultado. | A contagem inclui todos os símbolos do lado direito, mesmo os não usados no cálculo. |
| **Regra gramatical** | Análise Sintática | Descrição formal que define como sentenças são formadas a partir de símbolos terminais e não terminais. | Regra: `expr: expr PLUS expr { $$ = $1 + $3; }`<br>Instância: `x = 1 + 2` | Uma mesma regra pode ter **várias alternativas** para o lado direito, separadas pelo operador "\|" (equivalente a "ou"). Cada alternativa pode ter sua própria ação semântica. |
| **`yyparse()`** | Análise Sintática | Função gerada pelo Bison que realiza a análise sintática com base na gramática definida. | `int main(void) { return yyparse(); }` | Chama o lexer (`yylex`) repetidamente e aplica as regras da gramática. |
